// ===================
// package.json
// ===================
{
  "name": "fg-live-tracker",
  "version": "1.0.0",
  "description": "Redirect tracker for fg.live",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}